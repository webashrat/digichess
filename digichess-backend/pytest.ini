[pytest]
DJANGO_SETTINGS_MODULE = config.settings_test
python_files = test_*.py *_tests.py
addopts = -ra
markers =
    integration: tests that require external runtime stack (docker, redis, celery, beat, backend server)
